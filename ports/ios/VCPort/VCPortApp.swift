import SwiftUI

@main
struct VCPortApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
                .tint(Color(red: 10 / 255, green: 108 / 255, blue: 206 / 255))
        }
    }
}
